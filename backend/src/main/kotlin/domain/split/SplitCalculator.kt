package domain.split

import domain.receipt.Receipt

object SplitCalculator {

    fun splitEvenly(receipt: Receipt, peopleCount: Int): SplitResult {
        throw NotImplementedError("splitEvenly not implemented yet")
    }
}
